import KeyValueTable from './../../components/KeyValue/KeyValueTable';

export default function stylingSettings({ ctx, configType }: any) {
    return (
        <KeyValueTable ctx={ctx} configType={configType}/>
    );
}
